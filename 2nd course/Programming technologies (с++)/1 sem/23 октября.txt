#include<iostream>
using namespace std;

void f1()
{ //исправить мин и макс не должны быть заданы
    int i, n, min, imin, max, imax, x;
    cin>>n;
    min=50;
    max=-50;
    for(i=1;i<=n;i++){
        x=rand()%100-50;
        if(x>max){
            max=x;
            imax=i;
        }
        if(x<min){
            min=x;
            imin=i;
        }
    }
    cout<<min<<endl<<imin<<endl<<max<<endl<<imax;
}

void f2()
{
    int i, x, b, c, max, imax;
    cin>>b>>c;
    max=b;
    for(i=1;i<=10;i++){
        x=rand()%100-50;
        if ((x>max)and(x<c)){
            max=x;
            imax=i;
        }
    }
    cout<<max<<endl<<imax;
}

void f3()
{
    int i, x, n, max, imax, min, imin;
    cin>>n;
    min=50;
    max=-50;
    for(i=1;i<=n;i++){
        x=rand()%100-50;
        if(x>max){
            max=x;
            imax=i;
        }
        if(x<min){
            min=x;
            imin=i;
        }
    }
    if(imin>imax){
        cout<<imin;
    }
    else{
        cout<<imax;
    }
}

void f4()
{
    int i=1, n;
    double s=0, a;
    cin>>n;
    for(i=1;i<=n;i++){
        a=(i+1)/(2.0*i);
        s+=a;
    }
    cout<<s;
}

void f5()
{
    int i, n;
    double a, d, mas[15];
    cin>>n>>a>>d;
    mas[0]=a;
    cout<<mas[0]<<' ';
    for(i=1;i<n;i++){
        mas[i]=mas[i-1]/(d*1.0);
        cout<<mas[i]<<' ';
    }
}

void f6()
{
    int i, n1, n2,f, f1, f2;
    double  mas[20];
    cin>>n1>>n2;
    f=0;
    f1=1;
    f2=1;
    for(i=0;i<20;i++){
        mas[i] = f1/(i*1.0);
        f = f1;
        f1 = f2;
        f2 = f+f1;
    }
    for(i=n1;i<n2;i++)
        cout<<mas[i]<<' ';
}

void f7()
{
    int i;
    double  s=0, x, mas[10];
    for(i=0;i<10;i++){
        x=(4.0*i)/5;
        mas[i]=x;
        s+=x;
    }
    for(i=0;i<10;i++)
        cout<<mas[i]<<' ';
    cout<<s;
}

void f8()
{
    int i, t=-1;
    double  s=0, x, mas[10];
    mas[0]=1;
    for(i=1;i<10;i++){
        t*=(-1);
        x=(t*mas[i-1])/3.0;
        mas[i]=x;
        s+=x;
    }
    for(i=0;i<10;i++)
        cout<<mas[i]<<' ';
    cout<<s;
}

int main()
{
    int n;
    cout<<"Саргсян 23.10.24";
    while(n){
        cin>>n;
        switch(n)
        {
            case 1: f1(); break;
            case 2: f2(); break;
            case 3: f3(); break;
            case 4: f4(); break;
            case 5: f5(); break;
            case 6: f6(); break;
            case 7: f7(); break;
            case 8: f8(); break;
        }
    }
    return 0;
}